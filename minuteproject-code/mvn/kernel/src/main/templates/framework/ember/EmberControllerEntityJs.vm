#exposeQuerySpecific()
#set($table=$query.inputBean)
#exposeVariableEntitySpecific()
#putImportQuerySDDBean()
#putImportSDDInputBean()
import Ember from 'ember';

import BaseController from '../base';

## import _ from 'lodash';

import config from '../../config/environment';
export default BaseController.extend({ //$className
	${model.name}Service : Ember.inject.service("${model.name}"),
	model(params) {
#set($classFormat=$formatUtils.getUppercaseUnderscore2($className))
#set($classFormatVar=$formatUtils.getJavaNameVariable($className))
        return this.get('${model.name}Service').${classFormatVar}(params);
    },
});
