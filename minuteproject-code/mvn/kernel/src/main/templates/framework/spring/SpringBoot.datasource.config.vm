#generatorSpecific()
#exposeVariableApplicationSpecific()
package $packageName;

import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.jdbc.DataSourceBuilder;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.jdbc.core.JdbcTemplate;
import javax.sql.DataSource;

@Configuration
public class $className {

    @Bean (name="${model.name}DataSource")
    @ConfigurationProperties(prefix="spring.datasource.${model.name}")
    public DataSource ${classVariable}DataSource() {
        return DataSourceBuilder.create().build();
    }

    @Bean(name="${model.name}JdbcTemplate")
    public JdbcTemplate ${classVariable}JdbcTemplate(@Qualifier ("${model.name}DataSource") DataSource dataSource) {
        return new JdbcTemplate(dataSource);
    }
}