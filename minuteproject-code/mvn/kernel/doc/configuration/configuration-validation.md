# Configuration Validation
## xsd validation

## Additional validation
### Query-filter
* Name: must reference a property (property value = '$' and name (case sensitive)) within the query
* all query properties must be referenced
